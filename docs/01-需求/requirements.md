# 开发需求清单

> 2026-09-14 打包验收补充：单文件中文名 exe 可单独移动；无配置时向导按钮清晰可点、长路径换行；--auto-close 在首启与已配置情况下均能完成验证。真无 Python 机器与真实账单对账需单独验收，本机通过隔离目录和移除 Python PATH 验证可移植启动。
> 默认四项指标、进度条和状态均在窗口内，状态不能与指标重叠；错误换行后也应完整显示（Canvas 实际边界回归与 exe 截图验证）。

> 2026-09-14 阶段 2 修订：R2.1 的新鲜输入为内部字段，默认界面输入为总输入；R2.4 改为 time_created（缺失回退 time_updated）。官网 token 优先，本机补缺；官网费用与 token 缓存取日期并集。缓存校验、配置路径生效、分页完整性和 workspace 隔离以回归单测验收；本次不标记 UI 或打包验收完成。
> 配置验收补充：无效字段类型/范围不导致启动失败，旧输入字段迁移、窗口尺寸和首次提示状态可读写保留。

> 来源：设计文档 `docs/superpowers/specs/2026-08-12-opencode-usage-monitor-design.md`（需求以此为准）

## R1 数据

- [x] R1.1 读取本地 `opencode.db`（SQLite 只读模式），默认路径 `%USERPROFILE%\.local\share\opencode\opencode.db`（阶段 2 已实现）
- [x] R1.2 探测顺序：默认路径 → 常见备选 → 用户手动指定；设置窗可改（探测已实现，设置窗在阶段 5）
- [x] R1.3 刷新策略（实测修正）：全量读 `session` 表（毫秒级），预留 `max(time_updated)` 增量依据（阶段 2 已实现）
- [x] R1.4 db 被锁定/损坏：抛出 DbError 由上层处理（上层 UI 兜底在阶段 4/5）
- [x] R1.5 db 缺失/损坏时读缓存历史：缓存模块已实现，数据管线在阶段 5 接入

## R2 指标（口径与官网一致）

- [x] R2.1 输入 = 新鲜 inputTokens（实际发送口径，与 `opencode stats` 的 Input 一致；2026-08-12 用户确认修改，原"含缓存"口径废弃）
- [x] R2.2 输出 = outputTokens（阶段 2 已实现）
- [x] R2.3 命中率 = cache_read ÷ 总输入 × 100%（阶段 2 已实现）
- [x] R2.4 会话按 time_updated 归入自然日；周 = 周一至周日；月 = 自然月（阶段 2 已实现）
- [x] R2.5 数字缩写：≥1000 显示 1.2K / 1.3M（阶段 2 已实现）

## R3 缓存

- [x] R3.1 `%APPDATA%\opencodego-token-watcher\monitor_cache.json`，结构 `{session_ids[], by_day{}}`（阶段 2 已实现）
- [x] R3.2 周/月聚合由 by_day 现场计算，不额外存（阶段 2 已实现）
- [x] R3.3 缓存可删除重建；db 损坏时历史仍可看（pipeline 回退缓存，阶段 4 完成）

## R4 界面

- [x] R4.1 迷你悬浮窗（250×182，Windows 剪贴板风白色圆角卡片，深浅色自适应）
- [x] R4.2 置顶（右键菜单可切换）
- [x] R4.3 半透明：`-alpha`（默认 0.78，设置窗可调 0.50~0.95）+ `-transparentcolor` 抠角圆角（真 Acrylic 实测导致内容不绘制，弃用）
- [x] R4.4 可拖拽（按住任意处拖动）
- [x] R4.5 日/周/月标签切换，高亮当前粒度，切换回到最新
- [x] R4.6 ◀ ▶ 按当前粒度翻历史；点范围文字弹月历跳转（切到"日"粒度）
- [x] R4.7 显示：输入 / 输出 / 命中率百分比 + 三段式进度条（≥70% 绿 / 30~70% 橙 / <30% 红）
- [x] R4.8 1 分钟自动刷新（设置窗可配 1/2/5/10/30 分钟）+ 右键菜单手动刷新；启动即刷
- [x] R4.9 右键菜单：刷新 / 切换置顶 / 设置（阶段 5 启用）/ 关于 / 退出
- [x] R4.10 界面中文

## R5 首启向导

- [x] R5.1 首次启动（无配置）弹置顶向导：自动探测 db 路径 → 确认 / 手动指路径 / 重新探测 → "开始使用"（阶段 5 完成，程序化+视觉验证）

## R6 配置与分发

- [x] R6.1 `%APPDATA%\opencodego-token-watcher\config.json`：db 路径（空=自动）、开机自启（默认关）、窗口位置（退出时记忆，启动恢复）
- [x] R6.2 开机自启：写注册表 Run 键，可开关（阶段 5 完成，写入/读取/清理验证通过）
- [x] R6.3 PyInstaller 单 exe（--onefile --noconsole），未装 Python 的 Windows 可双击运行（阶段 6 完成：13MB exe 独立运行+自动探测+渲染验证通过）
- [x] R6.4 运行期零第三方依赖（仅 stdlib）

## R8 界面与功能迭代（2026-08-12 晚全部已实现）

- [x] R8.1 深浅色主题：设置窗三选 跟随系统/浅色/深色（默认 system，注册表检测），配置键 `theme`；主窗/设置窗/日历/关于/向导全部跟随
- [x] R8.2 透明度滑条：设置窗 Apple 风格自定义滑块 0.50~0.95（默认 0.78），拖动实时预览主窗，配置键 `window_alpha`
- [x] R8.3 主窗尺寸 250×182；状态栏文字底距卡片底 14px；数字 Segoe UI 10 常规
- [x] R8.4 设置窗分节：连接（db 路径 + 测试读取 + ⓘ 说明）/ 外观（主题 + 透明度）/ 行为（刷新间隔 1/2/5/10/30 分钟、数字格式 缩写/完整、启动置顶、锁定窗口位置、开机自启）/ 关于（版本 0.2、打开数据目录）
- [x] R8.5 置顶修复：取消置顶时任务栏出现入口（WS_EX_APPWINDOW + hide/show 强制 shell 重评估）
- [x] R8.6 单实例：多开提示"程序已在运行"并退出（CreateMutexW；`--auto-close` 验证模式跳过）
- [x] R8.7 首启向导增强：失败三分（未安装/无数据/读失败）+ 跳过按钮 + 今日用量预览 + 官方文档入口
- [x] R8.8 程序图标：上升柱状图（绿/蓝/橙），assets/app.ico（16~256 七尺寸）
- [x] R8.9 锁定窗口位置：设置窗开关，锁定时不可拖动，配置键 `lock_position`
- [x] R8.10 无极缩放：拖右下角/右/下边缘任意缩放（180x120~600x400），尺寸记忆

## R9 官网费用同步（2026-08-12 新增，AB 方案）

- [ ] R9.1 费用数据以官网 opencode.ai 为准（用户要求"所有数据按官网来"；官网无公开 API，用登录 cookie 调 server function，见设计文档 §10）
- [ ] R9.2 新模块 `src/opencode_cloud.py`（仅 stdlib）：`fetch_usages` / `fetch_costs` / 按天费用聚合 / 错误分类
- [ ] R9.3 配置新增 `cloud_workspace_id` / `cloud_cookie` / `cloud_enabled`
- [ ] R9.4 官网费用按月拉取缓存 `monitor_cloud.json`（按天+按模型费用、last_sync），失败回退显示"—"
- [ ] R9.5 主窗显示字段可勾选"费用"（今日/周/月聚合，官网口径）
- [ ] R9.6 右键菜单"同步官网费用"手动触发
- [ ] R9.7 设置窗新增"官网同步"分区：workspace ID / cookie 输入 + 测试读取（显示最近 3 天费用）
- [ ] R9.8 验证：与官网页面数字一致（8/12 deepseek $4.58 + mimo $0.92 = $5.50）
- [ ] R9.9 显示要素（官网计价口径）：默认 输入总（含缓存）/输出/命中率/费用；自定义可选 新鲜输入/缓存读/缓存写/推理/总token/请求次数（2026-08-13 用户确认）
- [ ] R9.10 窗口宽固定高自适应：高度随要素行数变化

## R7 成功标准（验收）

- [ ] S1 `python main.py` 双击即出小窗，自动探测 db 并显示当日用量
- [ ] S2 日/周/月切换、翻历史、日历跳转可用；数字与 `opencode stats` 抽查 3 个日期一致
- [ ] S3 命中率三段颜色、缩写格式正确
- [ ] S4 置顶/毛玻璃/拖拽/右键菜单/开机自启可用
- [ ] S5 单 exe 在未装 Python 的 Windows 上可运行
- [ ] S6 运行期内存占用低（轻量）

## 非目标（YAGNI，不做）

云端同步 / 自动更新 / 多语言 / 按模型或会话细分报表 / 成本统计
