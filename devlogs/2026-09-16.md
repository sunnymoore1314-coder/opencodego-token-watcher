# 2026-09-16 开发日志

## 本次阶段

- v0.3.1 UI 完整显示迭代；先完成本地演示验证，随后按用户要求恢复 GitHub 源码发布并回复对应 Issue。

## 今日完成

- [x] 主监测窗按指标行、进度条和状态提示自动计算高度；最大高度提升到 560，旧配置过小时也会自动展开。
- [x] 设置页由单列滚动页面改为 780×680 双栏总览；外观、行为、连接、显示内容、官网同步、关于和保存/取消同时可见。
- [x] 修正“鼠标穿透（Ctrl+Alt+Shift+T 恢复）”文字截断。
- [x] 设置页显示版本 0.3.1，README、使用说明、总设计、需求、技术方案和 UI 规范已同步。
- [x] 增加 `启动v0.3.1演示.cmd`，双击后用本机 `D:\Python\pythonw.exe` 启动源码版应用。
- [x] 将本轮无法通过 Tcl/Tk 初始化的测试 EXE 移到 `build/failed-packages`，避免误点。

## 验证

- 73 项自动化测试全部通过。
- 默认四项、长错误提示、全部十项指标的 Canvas 边界测试通过；十项指标实测窗口为 250×414，内容完整。
- 双栏设置页截图检查通过：780×680 内所有常用分区及底部按钮可见，无滚动视口。
- 隔离 APPDATA 运行 `python main.py --auto-close 3` 返回 0，本地演示启动路径可用。
- 截图：`2026-09-16-v031-main.bmp`、`2026-09-16-v031-all-fields.bmp`、`2026-09-16-v031-settings.bmp`。

## 待办

- [ ] 如后续恢复单文件 EXE 交付，继续解决 PyInstaller/Tcl 初始化兼容问题后重新打包；当前不作为本地演示阻塞项。

## GitHub 发布

- [x] 将 v0.3.1 源码、文档和本地演示启动入口同步到公开仓库 `sunnymoore1314-coder/opencodego-token-watcher`。
- [x] 回复 `sunnymoore1314-coder/afterwork#1` 的界面显示问题，说明双栏设置页和主窗自动展开修复。
- [ ] 单文件 EXE 仍待后续完成 Tcl/Tk 兼容打包后再发布。
